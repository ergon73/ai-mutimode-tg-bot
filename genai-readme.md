# Техническое задание для Cursor AI
## Telegram AI Assistant с LLM и ролями

Этот документ содержит промпты для итеративной генерации проекта через Cursor IDE.

---

## 📌 Промпт 1: Базовая структура бота

**Когда использовать:** В начале работы, в пустом проекте  
**Как использовать:** `Ctrl+K` (или `Cmd+K`) → вставить промпт → Enter

```
Создай полнофункционального Telegram-бота на Python с использованием aiogram v3+ и OpenAI API.

═══════════════════════════════════════════════════════════
📋 ТРЕБОВАНИЯ К АРХИТЕКТУРЕ
═══════════════════════════════════════════════════════════

1. СТЕК ТЕХНОЛОГИЙ:
   - Python 3.10+
   - Асинхронный код (async/await)
   - aiogram 3.x для Telegram Bot API
   - openai (последняя версия) для работы с OpenAI API
   - pydantic-settings для конфигурации
   - python-dotenv для .env файлов

2. СТРУКТУРА ПРОЕКТА:
   telegram-ai-assistant/
   ├── main.py              # Точка входа, хендлеры команд
   ├── config.py            # Конфигурация через Pydantic Settings
   ├── memory.py            # Класс MemoryManager для управления памятью
   ├── llm_service.py       # Класс LLMService для работы с OpenAI
   ├── prompts.json         # Ролевые модели бота
   ├── requirements.txt     # Зависимости
   ├── .env.example         # Пример конфигурации
   ├── .gitignore           # Исключения для Git
   └── README.md            # Документация проекта

3. ПРИНЦИПЫ РАЗРАБОТКИ:
   - Чистый, модульный код
   - Комментарии на РУССКОМ языке
   - Обработка всех возможных ошибок
   - Логирование всех важных событий
   - Следование правилам из .cursorrules

═══════════════════════════════════════════════════════════
⚙️ ТРЕБОВАНИЯ К ФУНКЦИОНАЛУ
═══════════════════════════════════════════════════════════

1. КОНФИГУРАЦИЯ (config.py):
   Используй Pydantic Settings для загрузки переменных из .env:
   - BOT_TOKEN (str) - токен Telegram-бота
   - OPENAI_API_KEY (str) - ключ OpenAI API
   - OPENAI_MODEL (str) - модель LLM (по умолчанию "gpt-4o-mini")
   - MAX_HISTORY_MESSAGES (int) - количество сообщений в памяти (по умолчанию 10)

2. ПАМЯТЬ ДИАЛОГА (memory.py):
   Класс MemoryManager должен:
   - Хранить историю сообщений для каждого chat_id
   - Сохранять последние N сообщений (из MAX_HISTORY_MESSAGES)
   - Хранить текущий режим (роль) для каждого чата
   - Сохранять состояние в memory.json для персистентности
   - Загружать состояние при старте
   - Методы:
     * add_message(chat_id, role, content) - добавить сообщение
     * get_history(chat_id, limit=None) - получить историю
     * clear_history(chat_id) - очистить историю
     * set_mode(chat_id, mode) - установить режим
     * get_mode(chat_id) - получить текущий режим
     * save() - сохранить в файл
     * load() - загрузить из файла

3. РАБОТА С LLM (llm_service.py):
   Класс LLMService должен:
   - Использовать AsyncOpenAI клиент
   - Метод generate_response(messages) для генерации ответа
   - Возвращать текст ответа + информацию об использовании токенов
   - Обрабатывать ошибки API (rate limits, invalid requests, etc.)

4. РОЛЕВЫЕ МОДЕЛИ (prompts.json):
   Создай файл с минимум 3 ролями:

{
  "default_prompt": "assistant",
  "prompts": {
    "assistant": {
      "name": "🤖 Обычный помощник",
      "description": "Универсальный помощник для повседневных задач",
      "system_prompt": "Ты — дружелюбный и полезный ассистент. Отвечай кратко, по существу, используя эмодзи для наглядности. Помогай пользователю с любыми вопросами."
    },
    "developer": {
      "name": "💻 Помощник разработчика",
      "description": "Эксперт по программированию и технологиям",
      "system_prompt": "Ты — опытный разработчик-наставник. Пиши чистый код на Python с комментариями. Форматируй код в блоках ```python. Объясняй решения пошагово. Предлагай best practices."
    },
    "analyst": {
      "name": "📊 Аналитик данных",
      "description": "Специалист по анализу и структурированию информации",
      "system_prompt": "Ты — аналитик данных. Структурируй ответы списками и таблицами. Делай логичные выводы на основе данных. Предлагай конкретные шаги и метрики для измерения результата."
    }
  }
}

5. TELEGRAM ХЕНДЛЕРЫ (main.py):

   a) Команда /start:
      - Приветствие с описанием возможностей
      - Показать текущий режим
      - Предложить команды: /mode, /reset

   b) Команда /mode:
      - Показать список доступных ролей через InlineKeyboard
      - Каллбэки вида "mode:{key}"
      - При выборе подтвердить смену режима

   c) Команда /reset:
      - Очистить историю диалога для текущего чата
      - Отправить подтверждение

   d) Обработка текстовых сообщений:
      - Показывать "typing..." пока генерируется ответ
      - Формировать контекст: system_prompt + история
      - Отправлять запрос в OpenAI
      - Сохранять вопрос и ответ в память
      - Возвращать ответ пользователю

6. ЛОГИРОВАНИЕ:
   - Настроить базовое логирование (logging)
   - Уровень: INFO
   - Формат: "%(asctime)s - %(levelname)s - %(message)s"
   - Логировать:
     * Старт бота
     * Получение сообщений
     * Смену режимов
     * Ошибки API

═══════════════════════════════════════════════════════════
📦 ДОПОЛНИТЕЛЬНЫЕ ФАЙЛЫ
═══════════════════════════════════════════════════════════

1. requirements.txt:
aiogram==3.13.1
openai==1.54.3
pydantic-settings==2.6.0
python-dotenv==1.0.1

2. .env.example:
# Telegram Bot
BOT_TOKEN=your_bot_token_here

# OpenAI API
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o-mini

# Settings
MAX_HISTORY_MESSAGES=10

3. .gitignore:
.env
memory.json
venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
.idea/
.vscode/

═══════════════════════════════════════════════════════════
🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ
═══════════════════════════════════════════════════════════

Сгенерируй ПОЛНОСТЬЮ РАБОЧИЙ проект со всеми указанными файлами.
Код должен быть чистым, с комментариями на русском языке.
После генерации проект должен запускаться командой:
python main.py

И работать сразу после установки зависимостей и настройки .env.
```

---

## 📌 Промпт 2А: Расчёт стоимости запроса в рублях

**Когда использовать:** После успешного создания базовой структуры  
**Как использовать:** Открыть `llm_service.py` и `main.py` → `Ctrl+L` → вставить промпт

```
Добавь функционал для расчёта и отображения стоимости каждого запроса к OpenAI API в рублях.

═══════════════════════════════════════════════════════════
📋 ТРЕБОВАНИЯ
═══════════════════════════════════════════════════════════

1. РАСЧЁТ СТОИМОСТИ:
   - После каждого ответа от LLM показывать статистику:
     * Количество входных (prompt) токенов
     * Количество выходных (completion) токенов
     * Стоимость в USD
     * Стоимость в RUB (по актуальному курсу ЦБ РФ)

2. ХРАНЕНИЕ ЦЕН:
   В llm_service.py создай словарь TOKEN_PRICES с ценами за 1 млн токенов:

# Цены OpenAI (за 1 миллион токенов)
TOKEN_PRICES = {
    'gpt-4o-mini': {
        'input': 0.15,    # $0.15 за 1M входных токенов
        'output': 0.60    # $0.60 за 1M выходных токенов
    },
    'gpt-4o': {
        'input': 5.00,
        'output': 15.00
    },
    'gpt-3.5-turbo': {
        'input': 0.50,
        'output': 1.50
    },
    'o1-mini': {
        'input': 3.00,
        'output': 12.00
    }
}

3. ПОЛУЧЕНИЕ КУРСА РУБЛЯ:
   Добавь асинхронную функцию для получения курса USD/RUB с API ЦБ РФ:

import aiohttp
import json

async def get_usd_rate() -> float:
    """
    Получить актуальный курс USD/RUB с сайта ЦБ РФ.
    Если не удалось получить - вернуть примерный курс 100.0
    """
    url = "https://www.cbr-xml-daily.ru/daily_json.js"
    try:
        async with aiohttp.ClientSession() as session:
            async with session.get(url, timeout=5) as response:
                if response.status == 200:
                    data = await response.json()
                    return data['Valute']['USD']['Value']
    except Exception as e:
        logging.warning(f"Не удалось получить курс ЦБ РФ: {e}")
    
    return 100.0  # Резервный курс

4. ОБНОВЛЕНИЕ LLMService:
   В классе LLMService добавь метод:

def calculate_cost(self, model_name: str, usage) -> dict:
    """
    Рассчитать стоимость запроса.
    
    Args:
        model_name: название модели
        usage: объект usage из ответа OpenAI
    
    Returns:
        dict с информацией о токенах и стоимости
    """
    input_tokens = usage.prompt_tokens
    output_tokens = usage.completion_tokens
    
    # Получить цены для модели
    prices = TOKEN_PRICES.get(model_name, TOKEN_PRICES['gpt-4o-mini'])
    
    # Расчёт стоимости в USD
    input_cost = (input_tokens / 1_000_000) * prices['input']
    output_cost = (output_tokens / 1_000_000) * prices['output']
    total_cost_usd = input_cost + output_cost
    
    return {
        'input_tokens': input_tokens,
        'output_tokens': output_tokens,
        'total_cost_usd': total_cost_usd
    }

5. ОБНОВЛЕНИЕ generate_response:
   Метод generate_response должен возвращать не только текст, но и статистику:

return {
    'text': response_text,
    'usage': {
        'input_tokens': usage.prompt_tokens,
        'output_tokens': usage.completion_tokens,
        'total_cost_usd': cost_data['total_cost_usd']
    }
}

6. ОБНОВЛЕНИЕ ХЕНДЛЕРА СООБЩЕНИЙ (main.py):
   После получения ответа от LLM:
   - Получить курс рубля
   - Рассчитать стоимость в рублях
   - Добавить статистику к ответу

Формат вывода:
```
[Ответ от бота]

💰 Стоимость запроса:
📥 Входные токены: 150
📤 Выходные токены: 85
💵 USD: $0.00007
💸 RUB: ~7.00₽
```

═══════════════════════════════════════════════════════════
📦 ДОПОЛНИТЕЛЬНЫЕ ЗАВИСИМОСТИ
═══════════════════════════════════════════════════════════

Добавь в requirements.txt:
aiohttp==3.10.5

═══════════════════════════════════════════════════════════
🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ
═══════════════════════════════════════════════════════════

После применения изменений бот должен после каждого ответа показывать детальную статистику по использованным токенам и стоимости запроса в USD и RUB.
```

---

## 📌 Промпт 2Б: Генерация изображений через GNAPI

**Когда использовать:** Альтернатива промпту 2А, если хочешь генерацию картинок  
**Как использовать:** Открыть `llm_service.py`, `main.py`, `prompts.json` → `Ctrl+L` → вставить промпт

```
Добавь функцию генерации изображений через GNAPI как дополнительный режим бота.

═══════════════════════════════════════════════════════════
📋 ТРЕБОВАНИЯ
═══════════════════════════════════════════════════════════

1. НОВЫЙ РЕЖИМ В prompts.json:
   Добавь четвёртый режим "image_generator":

"image_generator": {
  "name": "🎨 Генератор изображений",
  "description": "Создание изображений по текстовому описанию",
  "system_prompt": "Ты — генератор изображений. Когда пользователь описывает картинку, ты создаёшь её с помощью GNAPI. Перед генерацией кратко опиши, что будешь создавать."
}

2. КОНФИГУРАЦИЯ:
   Добавь в config.py:
   - GNAPI_KEY (str) - ключ API от GNAPI

   В .env.example добавь:
   # GNAPI (для генерации изображений)
   GNAPI_KEY=your_gnapi_key_here

3. СОЗДАНИЕ СЕРВИСА ImageService:
   Создай новый файл image_service.py с классом ImageService:

import aiohttp
import asyncio
import logging
from typing import Optional

class ImageService:
    """Сервис для работы с GNAPI для генерации изображений"""
    
    def __init__(self, api_key: str):
        self.api_key = api_key
        self.base_url = "https://api.gnapi.com/v1"
    
    async def generate_image(self, prompt: str) -> Optional[str]:
        """
        Генерация изображения по текстовому описанию.
        
        Args:
            prompt: описание желаемого изображения
        
        Returns:
            URL сгенерированного изображения или None при ошибке
        """
        # 1. Создаём задачу на генерацию
        task_id = await self._create_task(prompt)
        if not task_id:
            return None
        
        # 2. Ожидаем готовности (проверяем каждые 5 секунд)
        max_attempts = 60  # Максимум 5 минут
        for attempt in range(max_attempts):
            await asyncio.sleep(5)
            
            result = await self._check_status(task_id)
            if result:
                return result
        
        logging.error("Превышено время ожидания генерации изображения")
        return None
    
    async def _create_task(self, prompt: str) -> Optional[str]:
        """Создать задачу на генерацию"""
        url = f"{self.base_url}/images/generate"
        headers = {
            "Authorization": f"Bearer {self.api_key}",
            "Content-Type": "application/json"
        }
        data = {
            "model": "stable-diffusion-xl-1024-v1-0",
            "prompt": prompt,
            "width": 1024,
            "height": 1024
        }
        
        try:
            async with aiohttp.ClientSession() as session:
                async with session.post(url, headers=headers, json=data) as response:
                    if response.status == 200:
                        result = await response.json()
                        return result.get('task_id')
                    else:
                        logging.error(f"Ошибка создания задачи: {response.status}")
        except Exception as e:
            logging.error(f"Исключение при создании задачи: {e}")
        
        return None
    
    async def _check_status(self, task_id: str) -> Optional[str]:
        """Проверить статус задачи"""
        url = f"{self.base_url}/images/status/{task_id}"
        headers = {"Authorization": f"Bearer {self.api_key}"}
        
        try:
            async with aiohttp.ClientSession() as session:
                async with session.get(url, headers=headers) as response:
                    if response.status == 200:
                        result = await response.json()
                        status = result.get('status')
                        
                        if status == 'completed':
                            return result.get('image_url')
                        elif status == 'failed':
                            logging.error("Генерация изображения провалилась")
                            return None
        except Exception as e:
            logging.error(f"Ошибка проверки статуса: {e}")
        
        return None

4. ОБНОВЛЕНИЕ ХЕНДЛЕРА СООБЩЕНИЙ:
   В main.py добавь логику:
   - Если текущий режим == "image_generator":
     * Отправить "🎨 Начинаю генерацию изображения..."
     * Вызвать image_service.generate_image(text)
     * Отправить изображение через bot.send_photo(chat_id, image_url)
   - Иначе:
     * Стандартная обработка через LLM

5. ОБРАБОТКА ОШИБОК:
   - Если генерация не удалась:
     * Логировать ошибку
     * Отправить пользователю: "❌ Не удалось сгенерировать изображение. Попробуйте другое описание."
   - Если таймаут:
     * "⏱ Генерация заняла слишком много времени. Попробуйте упростить описание."

═══════════════════════════════════════════════════════════
🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ
═══════════════════════════════════════════════════════════

После применения изменений:
1. В /mode появится 4-й режим "Генератор изображений"
2. В этом режиме бот будет генерировать изображения по описанию
3. Пользователь получит картинку или сообщение об ошибке
```

---

## 📌 Промпт 3: Финальная полировка

**Когда использовать:** После того, как всё заработало  
**Как использовать:** `Ctrl+L` → вставить промпт

```
Проведи финальную проверку и полировку проекта:

1. ПРОВЕРЬ КОММЕНТАРИИ:
   - Все функции и классы должны иметь docstrings на русском
   - Сложная логика должна быть прокомментирована

2. ОБРАБОТКА ОШИБОК:
   - Все обращения к API обёрнуты в try/except
   - Логируются все важные ошибки
   - Пользователь получает понятные сообщения об ошибках

3. СОЗДАЙ README.md:
   Сгенерируй полноценный README.md для GitHub с разделами:
   - Название и описание проекта
   - Скриншот или гифка (описание того, как должен выглядеть)
   - Возможности (Features)
   - Технологии
   - Установка (пошагово)
   - Конфигурация (.env)
   - Использование (команды бота)
   - Структура проекта
   - Roadmap / TODO
   - Лицензия (MIT)
   - Контакты / автор

4. ПРОВЕРЬ .gitignore:
   Убедись, что в .gitignore есть:
   - .env
   - memory.json
   - venv/
   - __pycache__/
   - *.pyc
   - .DS_Store

5. ОПТИМИЗАЦИЯ:
   - Убери дублирующийся код
   - Проверь, что все импорты используются
   - Упрости слишком сложные функции

Цель: проект должен выглядеть профессионально и быть готов к публикации в портфолио.
```

---

## 🎓 Как использовать эти промпты

1. **Последовательно:** Сначала Промпт 1, потом один из Промптов 2 (А или Б), затем Промпт 3
2. **Итеративно:** Если что-то не работает — копируй ошибку, открывай соответствующий файл и через `Ctrl+L` проси исправить
3. **Адаптируй:** Можешь менять промпты под себя, добавлять детали

## 💡 Советы по работе с Cursor

- **`Ctrl+K`** — генерация нового кода "с нуля"
- **`Ctrl+L`** — чат с AI о текущем коде (нужно открыть файлы)
- **`.cursorrules`** — правила применяются автоматически ко всем запросам
- **Комментарии** — если AI сгенерировал код не так, попроси через `Ctrl+L`: "Перепиши эту функцию, чтобы..."

Удачи! 🚀
