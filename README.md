# Telegram AI Assistant

Учебный Telegram-бот на aiogram 3 с поддержкой LLM (OpenAI) и несколькими режимами ответов. Проект демонстрирует работу с памятью диалога, переключением ролей и расчётом стоимости запросов.

## Скриншот

> 📸 Добавьте сюда изображение с примером работы бота (команда `/mode` + ответ в режиме аналитика).

## Возможности

- 👥 Переключение между режимами: помощник, разработчик, аналитик
- 🧠 Короткая память на последние `N` сообщений для каждого чата
- 🧮 Отображение стоимости запроса в USD и рублях (через курс ЦБ РФ)
- ⏱ Индикация «typing…» во время генерации
- 🔄 Команда `/reset` для очистки памяти
- 📊 Логирование ключевых событий

## Технологии

- Python 3.12
- aiogram 3.13
- OpenAI API (`AsyncOpenAI`)
- `pydantic-settings`, `python-dotenv`
- `aiohttp`, `httpx`

## Установка

```bash
git clone https://github.com/<username>/telegram-ai-assistant.git
cd telegram-ai-assistant
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

## Конфигурация

Создайте файл `.env` (см. `.env.example`):

```
BOT_TOKEN=токен_бота
OPENAI_API_KEY=openai_key
OPENAI_MODEL=gpt-4o-mini
MAX_HISTORY_MESSAGES=10
# GNAPI_KEY=опционально
```

## Запуск

```bash
source .venv/bin/activate
python main.py
```

## Команды бота

- `/start` — приветственное сообщение и текущий режим
- `/mode` — выбор роли через inline-кнопки
- `/reset` — очистка истории чата
- Текстовые сообщения — отправляются в OpenAI с учётом выбранного режима и памяти

## Структура проекта

```
.
├── main.py
├── config.py
├── llm_service.py
├── memory.py
├── prompts.json
├── requirements.txt
├── .env.example
├── README.md
└── human-readme.md / genai-readme.md
```

## Roadmap

- [ ] Добавить режим генерации изображений через GNAPI
- [ ] Вести подсчёт стоимости в истории чата
- [ ] Написать авто-тесты для MemoryManager и LLMService

## Лицензия

Проект распространяется по лицензии MIT (см. `LICENSE`).

## Контакты

Автор: Georgy Belyanin  
Email: georgy.belyanin@gmail.com  
Telegram: [@GBDialogFlowRU_bot](https://t.me/GBDialogFlowRU_bot)

